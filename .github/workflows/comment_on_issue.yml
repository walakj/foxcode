name: Issue comment
on:
  issues:
    types: [opened]
    
jobs:
  comment_with_action:
    runs-on: ubuntu-latest
    steps:
      - name: Debug Context Step
        run: echo "${{ toJSON(github.event) }}"

